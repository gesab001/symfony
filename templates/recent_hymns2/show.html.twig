{% extends 'base.html.twig' %}

{% block title %}RecentHymns2{% endblock %}

{% block body %}
    <h1>RecentHymns2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Number</th>
                <td>{{ recent_hymns2.number }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ recent_hymns2.title }}</td>
            </tr>
            <tr>
                <th>Verses</th>
                <td>{{ recent_hymns2.verses }}</td>
            </tr>
            <tr>
                <th>AddedDate</th>
                <td>{{ recent_hymns2.addedDate ? recent_hymns2.addedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Popularity</th>
                <td>{{ recent_hymns2.popularity }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ recent_hymns2.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recent_hymns2_index') }}">back to list</a>

    <a href="{{ path('recent_hymns2_edit', {'id': recent_hymns2.id}) }}">edit</a>

    {{ include('recent_hymns2/_delete_form.html.twig') }}
{% endblock %}
