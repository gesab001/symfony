{% extends 'base.html.twig' %}

{% block title %}Sabbath{% endblock %}

{% block body %}
    <h1>Sabbath</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sabbath.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ sabbath.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sabbath.image }}</td>
            </tr>
            <tr>
                <th>Biblereference</th>
                <td>{{ sabbath.biblereference }}</td>
            </tr>
            <tr>
                <th>Word</th>
                <td>{{ sabbath.word }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sabbath_index') }}">back to list</a>

    <a href="{{ path('sabbath_edit', {'id': sabbath.id}) }}">edit</a>

    {{ include('sabbath/_delete_form.html.twig') }}
{% endblock %}
