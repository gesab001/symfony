<form method="post" action="{{ path('egw_egw_writings_complete_delete', {'id': egw_writings_complete.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ egw_writings_complete.id) }}">
    <button class="btn">Delete</button>
</form>
